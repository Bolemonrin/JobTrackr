# Copy this file to .env.local and fill in the value.
# Client secret of the "Web application" OAuth client in Google Cloud Console.
# Inlined into the bundle at build time — never commit the real value.
VITE_GOOGLE_CLIENT_SECRET=
